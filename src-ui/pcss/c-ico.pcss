
c-ico {
  fill: var(--ico-light);
  width: 1.5rem;
  height: 1.5rem;

  svg {
    display: block;
    width: 100%;
    height: 100%;
  }

  /* c-ico can be marked as 'action' in this case, act like a icon button */
  &.action:hover{
    cursor: pointer;
    fill: var(--ico-hover);
  }
}